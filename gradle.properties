# =============================================================================
# KmpToolkit — Single version source of truth
# Bump this ONE value to release all modules together.
# Current: 3.3.0 (toolkit-wide). Next patch: 3.3.1 | minor: 3.4.0 | major: 4.0.0
# After this release, mbl-actionhub-bump-version auto-opens a PR bumping
# this value to the next patch — manual edits should be rare from now on.
# =============================================================================
kmptoolkit.version=3.5.22

# Maven Central Publishing (vanniktech 0.36.0 reads these from gradle.properties)
SONATYPE_HOST=CENTRAL_PORTAL
SONATYPE_AUTOMATIC_RELEASE=true

#Gradle
org.gradle.jvmargs=-Xmx4096M -Dfile.encoding=UTF-8 -Dkotlin.daemon.jvm.options\="-Xmx4096M"
org.gradle.caching=true
org.gradle.configuration-cache=true
#Dokka — opt into v2 plugin (DokkaExtension + dokkaGenerate task). Required by
# the kmptoolkit DokkaConventionPlugin in build-logic/convention/.
org.jetbrains.dokka.experimental.gradle.pluginMode=V2EnabledWithHelpers
#Kotlin
kotlin.code.style=official
#MPP
kotlin.mpp.enableCInteropCommonization=true
#Android
android.useAndroidX=true
android.nonTransitiveRClass=true
#Compose Multiplatform
org.jetbrains.compose.experimental.macos.enabled=true
# Allow Homebrew JDK distributions for `createDistributable` (developer-machine workflow).
# CI release pipelines use a vendor-pinned Corretto/Temurin JDK where this flag is a no-op.
compose.desktop.packaging.checkJdkVendor=false
